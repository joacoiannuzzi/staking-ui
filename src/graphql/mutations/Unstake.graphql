mutation Unstake(
  $transactionId: ID!
  $amount: Int!
  $jobApplicationId: String!
) {
  unStake(
    transactionID: $transactionId
    amount: $amount
    jobApplicationID: $jobApplicationId
  )
}
